<?xml version="1.0" encoding="utf-8"?>
<!--
  Locales Stela ships translations for. On Android 13+ this gives users a per-app
  language picker in system settings for free. Add a <locale> entry per language as
  translations land.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
</locale-config>
